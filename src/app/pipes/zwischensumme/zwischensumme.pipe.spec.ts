import { ZwischensummePipe } from './zwischensumme.pipe';

describe('ZwischensummePipe', () => {
  it('create an instance', () => {
    const pipe = new ZwischensummePipe();
    expect(pipe).toBeTruthy();
  });
});
