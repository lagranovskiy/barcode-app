import { KorrekteAntwortenPipe } from './korrekte-antworten.pipe';

describe('KorrekteAntwortenPipe', () => {
  it('create an instance', () => {
    const pipe = new KorrekteAntwortenPipe();
    expect(pipe).toBeTruthy();
  });
});
